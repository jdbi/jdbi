language: java

jdk:
  - oraclejdk8

sudo: false

cache:
  directories:
    - '$HOME/.m2/repository'

before_install:
    - cp src/build/travis-toolchains.xml ~/.m2/toolchains.xml

install: true
script: mvn -B verify

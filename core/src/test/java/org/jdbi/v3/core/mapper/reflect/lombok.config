#
# Propagate the JDBI annotations onto the constructor arguments and getter/setters
# in the lombok generated classes. Otherwise, JDBI can not find them.
#
lombok.copyableAnnotations += org.jdbi.v3.core.mapper.Nested
lombok.copyableAnnotations += org.jdbi.v3.core.mapper.PropagateNull

